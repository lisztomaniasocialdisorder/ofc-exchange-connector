[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ofc-exchange-connector"
version = "0.1.0"
description = "Public review scope for OFC exchange API connector safety."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "OFC" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
